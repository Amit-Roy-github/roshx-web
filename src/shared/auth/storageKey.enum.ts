/** Everything this app keeps in localStorage, namespaced so nothing collides. */
export enum StorageKey {
    ACCESS_TOKEN = 'roshx-access-token',
    REFRESH_TOKEN = 'roshx-refresh-token',
    USER = 'roshx-user',
}
